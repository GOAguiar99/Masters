# Full-Scan Dissertation Review — Post-Measurement-Fix Congruence Pass

> **Trigger:** Implementation (Ch. 5), Evaluation (Ch. 6), and Conclusion (Ch. 7) were
> restructured around the measurement-fix campaign (`5b0f584`) and the hotspot campaign
> (`da0d616`). Goal of this pass: verify the **whole** document tells the new
> *regime-conditional protection* story congruently.
> **Review date:** 2026-08-16 · **Base commit:** `f183a45` (plus wave-A fixes applied in this pass)
> **Ground truths used:** `context/eval-kpis/{highway_light,highway_heavy,highway_heavy_hotspot}/`
> (parser `rust-kpi-dashboard-0.2.1`, schema 3) and the live artifact
> `../veins-inet-qos/veins_qos` (branch `fix/measurement-poisson-vo-clock`, HEAD `da0d616`).
> **Supersedes:** `DISSERTATION_REVIEW.md` (2026-08-14; pre-fix campaign numbers, now stamped).

---

## 1. Overall verdict

**Congruent after the wave-A front-matter fixes.** The rebuilt Evaluation/Conclusion story —
Voice is policy-invariant under uniform Poisson load (P95 ≈ 2.2 ms, floored by burst
self-queueing); emergency preemption recovers Voice service only under concentrated
hotspot contention with dense bursts (−39% mean / −21% P95 / −30% Voice collision losses);
bounded blocking without dropping only taxes BE — is now told consistently from Abstract to
Conclusion. **Every number in every Evaluation table matches the archived KPI rows exactly**
(~120 cells + derived percentages + seed ranges), and every new code claim matches the
artifact source. Six should-fix content/layout items remain (§5, wave B), none of which
contradicts the evidence; they are wording, framing, and two layout overflows.

**Before this pass, three material gaps existed and were closed in wave A (§6):**
the Abstract/Resumo/Intro contributions presented only the uniform-load *null* half of the
story and omitted the hotspot *gain* that the Evaluation headline calls "the contribution";
the KPI-archive README and hotspot note carried stale pins/values; the old review document
silently encoded the superseded pre-fix numbers.

---

## 2. What changed (context for this review)

| Campaign | Commit(s) | Effect on dissertation |
|---|---|---|
| Measurement fix | `5b0f584` (`volatile` Poisson BE redrawn per packet; VO stamped at logical burst start = alert age; `beEndToEndDelayFromCrash` slice) | Ch. 4/5/6 rewritten; pre-fix "−49% VO P95" headline retracted |
| Uniform-load rerun | `5b0f584` traces | VO policy-invariant (P95 2.21–2.25 ms); policy contrast is BE cost (stable 30 ms P95; emergency 0.37 ms via preemption) |
| Hotspot (S1) campaign | `abf1922`…`0cbc984`, merged at `da0d616` | New Ch. 6 §"Hotspot Load", `fig_08`/`fig_09`, `context/eval-kpis/highway_heavy_hotspot/`; emergency recovers VO (−39%/−21%/−30%) |

---

## 3. Story congruence matrix (claim by location)

| # | Location | Claim as written | Status |
|---|----------|------------------|--------|
| 1 | Resumo / Abstract | Conditional two-regime headline: flat 2.2 ms under uniform Poisson; −39%/−21% + 30% fewer VO losses under hotspot; "regime-conditional frontier, event-triggered escalation only" | ✅ **fixed in this pass** (was null-half only) |
| 2 | Intro — question/scope/objectives | Two-class BE/VO question + cost; objectives map 1:1 to Ch. 4–6 | ✅ aligned (regime-neutral) |
| 3 | Intro — Contribution 3 | Same conditional two-regime guidance as Abstract | ✅ **fixed in this pass** |
| 4 | Background | 25 ms budget framing; DCF no-differentiation caveat forwards to evaluation; EDCA "statistical, no bounds"; adaptive-load hook; DoS caveat | ✅ aligned; one framing nuance (finding 5.6) |
| 5 | Related Work | Gap = local, event-triggered, joint VO-protection/BE-cost evaluation across regimes; "protection-versus-cost as primary result" | ✅ aligned |
| 6 | System Model | Traffic/controller/event model; no promised VO gains; "exponential draws in the software artifact" | ✅ aligned (matches `volatile` Poisson) |
| 7 | Implementation | `volatile` Poisson statement; logical-clock ("alert age") statement; BE-from-crash slice; `_hotspot3` overlay with `extends`-ordering note | ✅ all verified against code (§4.2) |
| 8 | Evaluation — headline/`sec:eval:light`/`hotspot`/`cost` | "The answer is conditional, and the condition is the contribution" | ✅ aligned with every table (§4.1) |
| 9 | Evaluation — summary (`sec:eval:summary`) | "Recovers Voice service *only* when concentrated + dense; conditional practicality" | ✅ aligned |
| 10 | Conclusion — findings/takeaway/limits/future | Mirrors evaluation regime-conditionally; predictive-timings negative result referenced to artifact history | ✅ aligned (verified in git history) |
| 11 | Future work | Duty-cycle arming trigger references `sec:eval:disc:trigger` + `tab:eval:hotspot_dose` | ✅ aligned |

No stale pre-fix numbers remain anywhere in chapters or front matter (regex sweep for the
old headline set — 0.896→0.461 ms, −49%, 57.4→98.7 ms, +4854.7/+9665.7, "deterministic
interval" — returned no hits; the coincidental value 0.896 in `tab:eval:heavy_qos` is
*guarded BE mean* from the new archive).

---

## 4. Verified correct in this pass

### 4.1 Numbers (Evaluation tables vs. `context/eval-kpis/` archive)

**Method:** recomputed every table cell from `run_rows.json` (3-seed arithmetic means,
min–max ranges) and cross-checked `config_summary.json`; recomputed all derived
percentages. **Result: 11/11 tables match exactly; 0 discrepancies.**

| Table | Cells checked | Result |
|---|---|---|
| `tab:eval:light_abs` (light/high, 20 values + ranges) | all | ✅ exact |
| `tab:eval:light` (16 derived %) | all | ✅ exact (incl. +1553.4%, +11043.7%, −15.5%) |
| `tab:eval:heavy_qos` (40 values + ranges) | all | ✅ exact |
| `tab:eval:heavy_vo_loss` | all | ✅ exact (drops 16.2/15.6/19.6/19.8/16.0 ×10³; VO 3.7/3.8 ×10³; overflow 0) |
| `tab:eval:heavy_drop_attr` | all | ✅ exact (rows sum to totals) |
| `tab:eval:heavy_vo_rx` (15 values) | all | ✅ exact (9.945–9.954) |
| `tab:eval:hotspot` (30 values + P95 seed ranges) | all | ✅ exact (14.56, 17.8k, 16.1k, 998, 1453/1386/981/3.335/1.548; per-seed P95 sets match; emergency range [11.8–16.0] does not overlap baselines' minima) |
| `tab:eval:hotspot_dose` (6 derived %) | all | ✅ exact (−38.7→−39%, −21.1→−21%, +10.4→+10%, +4.5→+5%, +15.9→+16%, +0.2→+0%) |
| `tab:eval:heavy_p95` (45 cells + ranges) | all | ✅ exact (BE-from-crash row incl. 13.763/13.541–13.955) |
| `tab:eval:heavy_drop_rate` (15 values) | all | ✅ exact |
| `tab:eval:heavy_ctrl` | all | ✅ exact (115/115/127 ×10³; 2.53 ×10³; 12 holds) |

**Prose numbers** (Abstract/Intro/Eval/Conclusion) spot-verified against the same archive:
"within 0.08 ms of plain" (max |Δ| = 38 µs), "few tens of microseconds", "~800 packets/s",
"~22k BE TX/run" (archive 22118–22230), "72×10³ hotspot TX" (71650), "~400 VO pkt/s",
"2.5×10³ drops while blocked" (2527), "1.1×10⁵ activations" (114772–114894), "0.98–1.45 s
starvation" (981–1453 ms), "twelve suppressed grants" (12.3), title block, and all Conclusion
headline figures. **All pass.**

### 4.2 Artifact code claims (vs. `veins_qos` @ `da0d616`)

| Claim in dissertation | Repository evidence | Result |
|---|---|---|
| BE inter-arrivals Poisson, `volatile`, redrawn per packet | `CritPacketSender.ned:26–28` (`volatile double sendInterval`), `.cc:94` re-reads `par` | ✅ |
| VO delay stamped at logical burst start (alert age) | `CrashBurstApp.cc:145,167,176–178` (`logicalCreationTime` → `CreationTimeTag`) | ✅ |
| Crash-node BE slice `beEndToEndDelayFromCrash` | `CritPacketSender.ned:22`, `.cc:213–215` (src-address filter, `crashNodeIndex=0`) | ✅ |
| `_hotspot3`: nodes 1–3, `exponential(4ms)`, 1200 B; composed before `_netload_*` (first-match-wins) | heavy `omnetpp.ini:247–255` + precedence comment + 15 composed `<policy>_hotspot_<load>` configs | ✅ |
| Predictive pre-blocking implemented and ablated (Conclusion) | Added `9f850ae`, reverted `6189876`/`6370671` ("guarded regression"); no `predictive` remnant in `src/` or configs | ✅ |
| `tab:impl:parameters` MAC/EDCA values | `omnetpp.ini:121–179` (queues 128/128/64/32, AIFSN 7/3/2/2, cwMin 15/15/7/3, cwMax 1023/1023/15/7; stable 15/80/5/2; guarded 4/20/4/3; emergency 10/60/5/1 + preemption) | ✅ unchanged |
| Commit pin for traces | `5b0f584` **is** ancestor of `da0d616`, but hotspot configs only exist later → pin `da0d616` (docs/gitignore-only afterwards) | ⚠️ see finding 5.3 |

### 4.3 Structure, citations, figures, glossary

- **Numbering:** TOC = Intro roadmap (1 Intro … 7 Conclusion); `sec:eval:*`/`sec:conc:*` cross-references all resolve; **0 undefined refs, 0 undefined citations** in `build/main.log`.
- **Citations:** 45 cited keys / 45 bib entries — sets identical.
- **Figures:** 18 referenced files, all present; `fig_08`/`fig_09` label sets and embedded values match the hotspot archive (text audit of SVG exports).
- **Glossary:** 0 `\gls` uses without declaration (83 used / 86 declared; 3 unused declarations, see polish items).
- **RESUMO/ABSTRACT:** now tell the identical conditional story in both languages.

---

## 5. Issues found, ranked

### 5.1 Should-fix (wave B)

1. ~~**Limitations contradict multi-crasher evidence (content).**~~ **RESOLVED by author
   decision.** The author declared the 36-run multi-crasher campaign unreliable ("results are
   bad") and asked for its removal. References deleted from `EVAL_HOTSPOT_VO_PROTECTION.md`
   (§2.2 evidence bullet, §4 invariance scope, §5 reproducibility); the artefact git history
   (`6189876`/`6370671` rollbacks) already records the discard. The dissertation's "not
   evaluated" limitation statements (`5-chapter.tex:566`, `6-chapter.tex:57,84`) therefore
   stand as correct scope, unchanged.
2. ~~**Trace commit pin incomplete (chapters).**~~ **FIXED.** `chapters/5-chapter.tex:28` now
   pins tag **`dissertation-2026-08` = commit `da0d616`**; the annotated tag was created at
   `da0d616` and pushed to `origin` (`GOAguiar99/veins-inet-qos`). `context/eval-kpis/README.md`
   documents the same pin.
3. ~~**Mixed baseline attribution.**~~ **FIXED.** `chapters/5-chapter.tex:525` now reads
   "(−39% mean and −21% P95 versus plain DCF; −30% Voice-attributed collision losses versus
   EDCA alone)".
4. ~~**Background 25 ms framing predates the hotspot regime.**~~ **FIXED.**
   `chapters/1-chapter.tex:48` now adds "; when concentrated contention pushes baseline delays
   toward that bound, the claim becomes tail recovery against it".
5. **Invisible stress-overlay runs — OPEN (optional, low).** With the multi-crasher removed,
   the only unreported robustness evidence is the `stress=exp(25ms)` overlay (local artifact
   results, not in the KPI archive). If you want it on record: one sentence in
   `sec:eval:setup` or an `eval-kpis` archive entry. Not required for congruence.

### 5.2 Layout (mechanical)

6. ~~**Five overfull hboxes in `chapters/4-chapter.tex`.**~~ **FIXED** (all four real boxes):
   `\path{}` for the breakable package/file names (`veins_qos/simulations/`, `IdealObstacleLoss`,
   `highway.obstacles.xml`, legacy package dirs) and `\footnotesize` for the two config
   `\ttfamily` quote blocks (classifier wiring, MAC typename). Verified gone from the log:
   86 pt → 0, 81.3 pt → 0, 28.4 pt → 0, 8 pt → 0. One pre-existing 0.56 pt overflow in
   *body prose* of `chapters/5-chapter.tex` ("$3.7\times10^{3}$ for every QoS-enabled
   policy---") remains: 0.2 mm, invisible; rewording solely for it was judged not worth the risk.
7. **Pre-textual template overfulls** (capa/ficha/rosto/list area, 0.6–35 pt): pre-existing
   template behavior, out of chapter scope; noted for completeness only.

### 5.3 Polish (optional)

8. `chapters/4-chapter.tex:675` — "A **sixth** overlay family, `_hotspot3`": unexplained
   ordinal (only three `_netload_*` overlays are introduced). Propose "A further overlay family".
9. `chapters/5-chapter.tex:517` — `\label{sec:eval:disc:reach}` sits inside
   `sec:eval:disc:plain`, so both labels print the same subsection number. Harmless; consider
   a real (sub)subsection if you want a distinct printed anchor.
10. ~~**Orphaned figures**~~ **DONE.** Removed (`git rm`) after verifying zero references in
    any LaTeX source (only doc-list mentions): 11 legacy PNGs (`Topology`, `delay`,
    `classifier`, `mymodel`, `upperframe`, `lowerframe`, `noqos`, `edca_only`, `reservaonly`,
    `both`, `carcrashonly`) + `plain_class_p95.{tex,pdf}` (measurement-review figure, never
    referenced). `WRITING_ROADMAP.md` figure tables updated to record the removal.
11. `msdu` acronym declared but no longer used (only plain-text "MSDU" in Ch. 5 prose);
    `cch`/`sch` are used via `\gls*`. 3 unused declarations total.
12. `EVAL_HOTSPOT_VO_PROTECTION.md` §3.1 values corrected in this pass (14.76→14.56,
    −23%→−21%, 17.6k→17.8k) to match the archive; note kept as design record.

---

## 6. Fixed in this pass (wave A)

| File | Change |
|---|---|
| `beforetext/beforetext.tex` | **Abstract + Resumo** rewritten to the conditional two-regime headline (uniform-load null + hotspot gains −39%/−21%/−30% + regime-conditional frontier takeaway) |
| `chapters/0-Intro.tex` | **Contribution 3** rewritten identically (regime-conditional guidance) |
| `WRITING_ROADMAP.md` | statuses updated (abstract headline, evaluation structure, light=3 seeds, active-figure set incl. fig_08/09, orphan table state) |
| `context/eval-kpis/README.md` | added `highway_heavy_hotspot` package row + naming/ordering rule; pinned **`da0d616`** as the commit covering all three archives |
| `EVAL_HOTSPOT_VO_PROTECTION.md` | §3.1 headline values reconciled with the archive (+reconciliation note) |
| `DISSERTATION_REVIEW.md` | stamped **SUPERSEDED** (pre-fix numbers) with pointers to this review |

**Wave B (applied after author approval):** multi-crasher references removed from the
hotspot note (author: campaign unreliable); chapter trace pin → tag `dissertation-2026-08`
(created + pushed); F3/F4 wording fixes applied; 4-chapter layout overflows fixed;
13 orphaned figures removed (`git rm`); final rebuild clean (0 errors, 0 undefined refs).

Post-fix rebuild: **0 errors, 0 undefined references/citations**; overfull inventory unchanged
(only the pre-existing ones listed above).

---

## 7. Final recommendation

The manuscript is congruent with the new evidence base, and its evidence chain
(tables ⇄ JSON archive ⇄ artifact @ tag `dissertation-2026-08` = `da0d616`) verifies
end-to-end. Wave A (front-matter + housekeeping) and wave B items 1–6 were applied in this
pass; the build is clean (0 errors, 0 undefined refs/citations, no chapter-level overfull
hboxes beyond one invisible 0.2 mm body-prose case). Open items: the optional stress-overlay
record (5.1.5), three cosmetic polish items (8, 9, msdu declaration), and the pre-textual
template overflows. Remaining structural caveats (3 seeds, no CIs, two-class abstraction,
simulation-only) are honestly disclosed scope decisions, not defects.

**Watch-item for the record:** the SBESC/ETFA short papers still report the *pre-fix*
campaign (`WRITING_ROADMAP.md` notes this). The dissertation now deliberately supersedes
them (`roadmap` line updated). Keep this divergence in mind if a camera-ready or a
companion-paper update ever happens.

---

*Generated by a full scan of `main@f183a45` + wave-A edits; all numbers recomputed from
`context/eval-kpis/`; all code claims checked against `veins_qos@da0d616`.*
