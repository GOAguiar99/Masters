\chapter{Evaluation}
\label{chap:evaluation}

\autoref{chap:sysmodel} defined the crash-aware system conceptually, and \autoref{chap:implementation} mapped it onto the Veins/\gls{inet} artifact.
This chapter asks whether that realization works as intended: does crash-triggered Voice traffic obtain better wireless service than ordinary Best Effort under contention, and what cost does that protection impose on ordinary traffic?

The evaluation is a protection-versus-cost study, not a claim of deterministic real-time guarantees~\cite{kosekszott2012What,IEEE80211e_2005}.
Two highway-density packages are used: a light-density sensitivity sweep (10~vehicles) and a heavy-density congestion stress case (100~vehicles), with emphasis on the latter, which exposes the strongest separation~\cite{aguiar2026_veins_qos}.
Numeric bindings for timing, radio, load, and \gls{mac} policies are those of \autoref{tab:impl:parameters}; this chapter reports outcomes, not module wiring.

The chapter is organized as follows.
Section~\ref{sec:eval:setup} states the experimental matrix and seed budgets.
Section~\ref{sec:eval:metrics} defines metrics and aggregation.
Section~\ref{sec:eval:light} presents the light-density gating sweep.
Section~\ref{sec:eval:heavy} reports heavy-density stress results.
Section~\ref{sec:eval:discussion} discusses interpretation, practical significance, and limitations.
Section~\ref{sec:eval:summary} summarizes the conditional takeaway.

%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%
\section{Experimental Setup}
\label{sec:eval:setup}

All experiments use the consolidated bindings in \autoref{tab:impl:parameters} and the reproducibility scripts in the public artifact~\cite{aguiar2026_veins_qos}.
The light-density package runs the full matrix of five \gls{mac} policies and three offered-load profiles (15~configurations) with five OMNeT++ repetitions numbered \texttt{\#0}--\texttt{\#4}; the heavy-density package runs the same matrix with three repetitions numbered \texttt{\#0}--\texttt{\#2}.
Each configuration name follows \texttt{<policy>\_netload\_<low|medium|high>} as documented in \autoref{chap:implementation}.
Raw \texttt{.sca}/\texttt{.vec} traces remain in the artifact clone (commit \texttt{39155fa}); the compact per-run \gls{kpi} rows used to build the tables in this chapter are archived with the dissertation sources under \texttt{context/eval-kpis/}.

Contention is induced by offered load and fleet density rather than by synthetic random-waypoint mobility alone~\cite{veins_sommer2011}.
At \texttt{netload\_high}, each of the 100~vehicles emits Best Effort multicast with an exponential mean of~\SI{125}{\milli\second} (approaching 800~packets/s fleet-wide once all vehicles have entered; SUMO injects the fleet progressively over the first part of the run), while the crash source adds up to eight physical Voice repeats every~\SI{20}{\milli\second} during the alert window.
Because a burst of eight repeats spaced \SI{5}{\milli\second} apart spans about \SI{35}{\milli\second}, successive bursts overlap, and the alert flow is effectively a sustained stream of roughly 400~packets/s while the window is active.
The resulting stress is visible in \autoref{sec:eval:heavy}: Best Effort 95th-percentile (P95) delays of 57--174~ms, \gls{mac} drop totals of $251$--$485\times10^{3}$ per run, and Best Effort queue-overflow counts up to $197\times10^{3}$, confirming sustained channel contention~\cite{aguiar2026_veins_qos,Continuous_Backoff_Freezing_Li}.

%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%
\section{Metrics and Aggregation}
\label{sec:eval:metrics}

The primary metrics are class-specific end-to-end delay, jitter, multicast reach, and \gls{mac}-layer losses; the definitions below apply to the Evaluation tables in this chapter.

End-to-end delay runs from packet creation to reception at each receiving vehicle.
Mean delays aggregate per-run statistics across receivers, while tail and jitter statistics pool all receivers' per-packet samples, excluding locally looped-back copies.
The 95th percentile (\gls{p95}) is the main tail metric because rare but long delays dominate crash-alert usefulness; mean absolute jitter summarizes short-term delay variation on the same samples.

Because every packet is multicast, reception is a fan-out that can exceed one:
Voice \emph{RX/alert} (equivalently RX per logical transmission in the load ladder) divides received alert copies by logical alert sequences;
Best Effort \emph{RX/TX} divides received background copies by transmitted packets.
\gls{mac} analysis reports total drops, drops per application transmission, and per-access-category attribution (Best Effort, Voice, unclassified); the Voice-attributed drop count therefore quantifies crash-frame loss at the \gls{mac} layer, summed over all nodes and including receive-side discards of corrupted frames.
When the per-category counters sum to twice the aggregate drop counter, the analysis pipeline halves the attributed counts so that categories sum to the aggregate (a double-counting guard in the parser); attribution is unavailable under \texttt{plain}, where no classifier assigns access categories.
Controller instrumentation adds Voice protection activations, Best Effort drops while blocked, and held Best Effort grants (\autoref{chap:implementation}).

Each repetition yields one run-level \gls{kpi} row.
Configuration summaries in this chapter are arithmetic means over seeds; tail statistics are computed per run over the pooled receiver samples and then averaged across seeds (a mean of per-run percentiles).
Headline delay, jitter, and reach tables also report the minimum--maximum range across those seeds.
Heavy-density results use three seeds and light-density results five; \emph{no significance tests are reported}.
The numbers are therefore qualitative mechanism checks under a controlled matrix, not proven effect sizes~\cite{aguiar2026_veins_qos}.

%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%
\section{Light-Density Sensitivity Sweep}
\label{sec:eval:light}

The light-density sweep tests whether crash-aware protection is beneficial before the channel becomes saturated.
At \texttt{low} and \texttt{medium} offered load, Voice P95 stays sub-millisecond for every policy.
Best Effort P95 is likewise sub-millisecond except when untimely protection fires---notably \texttt{stable}, whose mean Best Effort P95 reaches 2.9~ms at \texttt{low} and 10.2~ms at \texttt{medium} because a subset of seeds opens a protection window without Voice-limited congestion.
The discriminative case is therefore \texttt{high} load with ten contending nodes.

\autoref{tab:eval:light_abs} reports the corresponding high-load delays in milliseconds (five-seed means and ranges).
\autoref{tab:eval:light} restates the same cells as percentages relative to \texttt{plain\_netload\_high}.
Positive percentages indicate degradation relative to that sub-millisecond baseline; negative values indicate improvement.
\autoref{fig:eval:p95_gap_light} shows the Best Effort versus Voice P95 gap at the same density.

\begin{table}[htb]
	\ABNTEXfontereduzida
	\caption{\label{tab:eval:light_abs}Absolute light-density high-load delay (ms; 10~vehicles, five-seed means with min--max range). These cells are the denominators of \autoref{tab:eval:light}.}
	\begin{center}
		\scriptsize
		\setlength{\tabcolsep}{4pt}
		\begin{tabular}{@{}lcccc@{}}
			\toprule
			& \multicolumn{2}{c}{\textbf{\gls{vo}}} &
			  \multicolumn{2}{c}{\textbf{\gls{be}}} \\
			\cmidrule(lr){2-3}\cmidrule(lr){4-5}
			\textbf{Policy} & \textbf{Mean} & \textbf{P95} & \textbf{Mean} & \textbf{P95} \\
			\midrule
			Plain     & \shortstack{0.332\\[-1pt]{\scriptsize [0.284--0.476]}} & \shortstack{0.657\\[-1pt]{\scriptsize [0.492--1.054]}} & \shortstack{0.392\\[-1pt]{\scriptsize [0.376--0.411]}} & \shortstack{0.513\\[-1pt]{\scriptsize [0.369--0.716]}} \\
			EDCA      & \shortstack{0.346\\[-1pt]{\scriptsize [0.275--0.603]}} & \shortstack{0.671\\[-1pt]{\scriptsize [0.438--1.460]}} & \shortstack{21.527\\[-1pt]{\scriptsize [0.377--106.076]}} & \shortstack{29.277\\[-1pt]{\scriptsize [0.369--144.463]}} \\
			Stable    & \shortstack{0.363\\[-1pt]{\scriptsize [0.275--0.617]}} & \shortstack{0.769\\[-1pt]{\scriptsize [0.433--1.508]}} & \shortstack{24.720\\[-1pt]{\scriptsize [2.635--106.484]}} & \shortstack{50.114\\[-1pt]{\scriptsize [12.103--144.835]}} \\
			Guarded   & \shortstack{0.361\\[-1pt]{\scriptsize [0.279--0.612]}} & \shortstack{0.740\\[-1pt]{\scriptsize [0.445--1.423]}} & \shortstack{22.176\\[-1pt]{\scriptsize [0.951--106.215]}} & \shortstack{33.757\\[-1pt]{\scriptsize [4.317--144.092]}} \\
			Emergency & \shortstack{\textbf{0.275}\\[-1pt]{\scriptsize [0.269--0.292]}} & \shortstack{\textbf{0.409}\\[-1pt]{\scriptsize [0.371--0.510]}} & \shortstack{25.142\\[-1pt]{\scriptsize [0.388--123.981]}} & \shortstack{25.426\\[-1pt]{\scriptsize [0.369--125.354]}} \\
			\bottomrule
		\end{tabular}
	\end{center}
	\fonte{Prepared by the author.}
\end{table}

\begin{table}[htb]
	\ABNTEXfontereduzida
	\caption{\label{tab:eval:light}Normalized light-density high-load results relative to the sub-millisecond \texttt{plain\_netload\_high} baselines in \autoref{tab:eval:light_abs} (10~vehicles, five seeds). Positive percentages indicate degradation; negative values indicate improvement.}
	\begin{center}
		\setlength{\tabcolsep}{5pt}
		\begin{tabular}{@{}lrrrr@{}}
			\toprule
			& \multicolumn{2}{c}{\textbf{\gls{vo} delay change}} &
			  \multicolumn{2}{c}{\textbf{\gls{be} delay change}} \\
			\cmidrule(lr){2-3}
			\cmidrule(lr){4-5}
			\textbf{Policy} & \textbf{Mean} & \textbf{P95} & \textbf{Mean} & \textbf{P95} \\
			\midrule
			EDCA      & $+4.4\%$  & $+2.2\%$   & $+5389.9\%$ & $+5605.2\%$ \\
			Stable    & $+9.4\%$  & $+17.1\%$  & $+6204.3\%$ & $+9665.7\%$ \\
			Guarded   & $+8.8\%$  & $+12.7\%$  & $+5555.4\%$ & $+6478.2\%$ \\
			Emergency & $\mathbf{-17.2\%}$ & $\mathbf{-37.6\%}$ & $+6311.9\%$ & $+4854.7\%$ \\
			\bottomrule
		\end{tabular}
	\end{center}
	\fonte{Prepared by the author.}
\end{table}

\begin{figure}[htb]
	\caption{\label{fig:eval:p95_gap_light}Best Effort versus Voice P95 delay at \texttt{netload\_high}, light density (10~vehicles). Absolute Voice tails remain near 1~ms; Best Effort inflates under every QoS-enabled policy.}
	\begin{center}
		\includegraphics[width=0.95\linewidth]{Figs/fig_01_p95_delay_priority_gap_highway_light.pdf}
	\end{center}
	\fonte{Prepared by the author.}
\end{figure}

\autoref{tab:eval:light_abs}, \autoref{tab:eval:light}, and \autoref{fig:eval:p95_gap_light} reveal a consistent pattern: marking and bounded adaptive profiles degrade crash-alert delay slightly while inflating Best Effort delay by orders of magnitude.
The mechanism is not failed Voice prioritization but \emph{untimely} protection: under mild congestion the controller still reacts to Voice activity and defers Best Effort access, so background packets queue although the channel is not yet Voice-limited (\autoref{sec:sys:controller}).
Only \texttt{emergency} reverses the Voice trend, because preemption removes stale Best Effort work instead of merely postponing it; the gain is small in absolute terms since the baseline Voice tail is already sub-millisecond.
The sweep is therefore a gating test: adaptive blocking without dropping is risky when protection windows can open under low channel stress.

%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%
\section{Heavy-Density Stress Results}
\label{sec:eval:heavy}

The heavy-density package stresses the shared medium with one hundred vehicles on the same corridor and load matrix.
The tables below expand the dashboard \gls{kpi} summary beyond the page-limited conference version: class-specific delay and jitter, Voice loss and reach, per-access-category drop attribution, and load ladders for normalized \gls{mac} loss and Voice reception.
Three figures carry the visual story that matters most for crash alerts---the high-load P95 priority gap (\autoref{fig:eval:p95_gap_heavy}), the Voice delay CDF (\autoref{fig:eval:vo_cdf}), and high-load drop attribution (\autoref{fig:eval:drop_attr}).
Controller activity is shown beside the corresponding table (\autoref{fig:eval:ctrl}).

\subsection{QoS separation under congestion}
\label{sec:eval:heavy:qos}

\begin{table}[H]
	\ABNTEXfontereduzida
	\caption{\label{tab:eval:heavy_qos}Heavy-density high-load delay, jitter, and multicast reach (100~vehicles, three-run means with min--max range across seeds; delays and jitter in~ms).}
	\begin{center}
		\scriptsize
		\setlength{\tabcolsep}{3pt}
		\resizebox{\linewidth}{!}{%
		\begin{tabular}{@{}lrrrrrrrr@{}}
			\toprule
			& \multicolumn{4}{c}{\textbf{\gls{vo} (crash alert)}} &
			  \multicolumn{4}{c}{\textbf{\gls{be} (background)}} \\
			\cmidrule(lr){2-5}\cmidrule(lr){6-9}
			\textbf{Policy} &
			\textbf{Mean} & \textbf{P95} & \textbf{Jitter} & \textbf{RX/alert} &
			\textbf{Mean} & \textbf{P95} & \textbf{Jitter} & \textbf{RX/TX} \\
			\midrule
			Plain     & \shortstack{0.575\\[-1pt]{\scriptsize [0.318--1.084]}} & \shortstack{1.290\\[-1pt]{\scriptsize [0.676--2.515]}} & \shortstack{0.321\\[-1pt]{\scriptsize [0.108--0.736]}} & \shortstack{9.068\\[-1pt]{\scriptsize [8.556--9.906]}} & \shortstack{27.589\\[-1pt]{\scriptsize [0.402--60.308]}} & \shortstack{57.367\\[-1pt]{\scriptsize [0.678--97.759]}} & \shortstack{5.572\\[-1pt]{\scriptsize [0.061--13.996]}} & \shortstack{6.153\\[-1pt]{\scriptsize [3.702--7.589]}} \\
			EDCA      & \shortstack{0.433\\[-1pt]{\scriptsize [0.303--0.672]}} & \shortstack{0.896\\[-1pt]{\scriptsize [0.581--1.479]}} & \shortstack{0.193\\[-1pt]{\scriptsize [0.083--0.385]}} & \shortstack{9.904\\[-1pt]{\scriptsize [8.697--10.738]}} & \shortstack{54.760\\[-1pt]{\scriptsize [30.148--76.566]}} & \shortstack{155.857\\[-1pt]{\scriptsize [145.173--172.139]}} & \shortstack{23.059\\[-1pt]{\scriptsize [15.077--33.377]}} & \shortstack{6.147\\[-1pt]{\scriptsize [4.888--6.871]}} \\
			Stable    & \shortstack{0.469\\[-1pt]{\scriptsize [0.340--0.683]}} & \shortstack{1.155\\[-1pt]{\scriptsize [0.812--1.646]}} & \shortstack{0.261\\[-1pt]{\scriptsize [0.152--0.413]}} & \shortstack{9.826\\[-1pt]{\scriptsize [8.743--10.500]}} & \shortstack{59.434\\[-1pt]{\scriptsize [3.634--108.413]}} & \shortstack{144.314\\[-1pt]{\scriptsize [30.734--251.945]}} & \shortstack{21.710\\[-1pt]{\scriptsize [2.740--32.235]}} & \shortstack{5.534\\[-1pt]{\scriptsize [3.948--6.747]}} \\
			Guarded   & \shortstack{0.443\\[-1pt]{\scriptsize [0.312--0.670]}} & \shortstack{0.960\\[-1pt]{\scriptsize [0.612--1.525]}} & \shortstack{0.207\\[-1pt]{\scriptsize [0.096--0.377]}} & \shortstack{9.778\\[-1pt]{\scriptsize [8.277--10.785]}} & \shortstack{74.938\\[-1pt]{\scriptsize [63.994--91.272]}} & \shortstack{174.401\\[-1pt]{\scriptsize [150.061--220.345]}} & \shortstack{25.430\\[-1pt]{\scriptsize [19.691--30.169]}} & \shortstack{3.644\\[-1pt]{\scriptsize [1.794--5.250]}} \\
			Emergency & \shortstack{\textbf{0.282}\\[-1pt]{\scriptsize [0.274--0.295]}} & \shortstack{\textbf{0.461}\\[-1pt]{\scriptsize [0.427--0.507]}} & \shortstack{\textbf{0.047}\\[-1pt]{\scriptsize [0.033--0.073]}} & \shortstack{9.912\\[-1pt]{\scriptsize [8.742--10.721]}} & \shortstack{60.664\\[-1pt]{\scriptsize [0.439--122.333]}} & \shortstack{98.663\\[-1pt]{\scriptsize [0.693--150.234]}} & \shortstack{23.331\\[-1pt]{\scriptsize [0.132--35.506]}} & \shortstack{5.021\\[-1pt]{\scriptsize [2.489--6.318]}} \\
			\bottomrule
		\end{tabular}%
		}
	\end{center}
	\fonte{Prepared by the author.}
\end{table}

\begin{figure}[htb]
	\caption{\label{fig:eval:p95_gap_heavy}Best Effort versus Voice P95 delay at \texttt{netload\_high}, heavy density (100~vehicles). Crash Voice remains near 1~ms while Best Effort tails stretch to tens--hundreds of milliseconds; emergency yields the lowest Voice P95 among the five policies.}
	\begin{center}
		\includegraphics[width=0.95\linewidth]{Figs/fig_01_p95_delay_priority_gap_highway_heavy.pdf}
	\end{center}
	\fonte{Prepared by the author.}
\end{figure}

\begin{figure}[htb]
	\caption{\label{fig:eval:vo_cdf}Empirical Voice delay distribution at \texttt{netload\_high}, heavy density (100~vehicles). Emergency compresses the tail most; EDCA alone shifts the distribution only slightly.}
	\begin{center}
		\includegraphics[width=0.95\linewidth]{Figs/fig_06_vo_delay_cdf_high_load_highway_heavy.pdf}
	\end{center}
	\fonte{Prepared by the author.}
\end{figure}

\autoref{tab:eval:heavy_qos} shows that EDCA lowers Voice P95 from 1.29~ms to 0.90~ms but leaves the distribution shape close to the non-\gls{qos} baseline; emergency lowers it further to 0.46~ms---about a 49\% reduction relative to EDCA---while also collapsing Voice jitter from 0.193~ms (EDCA) to 0.047~ms.
\autoref{fig:eval:p95_gap_heavy} makes the priority gap explicit under the same high-load regime, and \autoref{fig:eval:vo_cdf} confirms that emergency compresses the full Voice delay mass rather than only the mean~\cite{aguiar2026_veins_qos}.
Bounded \texttt{stable}/\texttt{guarded} improve mean Voice delay but leave Voice P95 closer to baseline, indicating that deferred Best Effort backlog can still affect later bursts.
Voice RX/alert stays in a high-single-digit band (about 9.07 under \texttt{plain} and 9.78--9.91 under the QoS-enabled policies), while Best Effort RX/TX falls from 6.15 under \texttt{plain} to 5.02 (emergency) and 3.64 (guarded).
The two ratios are not interchangeable: Voice divides received alert copies by logical alert sequences at a stopped crash node, whereas Best Effort divides received copies by physical transmissions averaged over the whole fleet, including the early sparse-injection phase (\autoref{sec:eval:disc:reach}).

\subsection{Voice loss and MAC drop attribution}
\label{sec:eval:heavy:mac}

Crash usefulness also depends on whether Voice frames are lost.
\autoref{tab:eval:heavy_vo_loss} isolates Voice-side loss and reach together with Best Effort queue overflow; \autoref{tab:eval:heavy_drop_attr} attributes aggregate \gls{mac} drops to Best Effort, Voice, or unclassified counters (dashboard drop-attribution export); \autoref{tab:eval:heavy_ctrl} records controller activity for the adaptive profiles.

\begin{table}[H]
	\ABNTEXfontereduzida
	\caption{\label{tab:eval:heavy_vo_loss}Heavy-density high-load Voice loss and Best Effort overflow (100~vehicles, three-run means; counts in thousands, $\times 10^{3}$). \emph{VO MAC drops} counts Voice-attributed \gls{mac}-layer drops over all nodes (\autoref{sec:eval:metrics}); lower is better for crash dissemination. Under \texttt{plain}, access-category attribution is unavailable (no classifier), so the Voice-drop and Best Effort overflow columns are not applicable.}
	\begin{center}
		\small
		\setlength{\tabcolsep}{5pt}
		\begin{tabular}{@{}lrrrr@{}}
			\toprule
			\textbf{Policy} &
			\textbf{VO MAC drops} &
			\textbf{VO RX/alert} &
			\textbf{Total MAC drops} &
			\textbf{BE queue overflow} \\
			\midrule
			Plain     & n.a.  & 9.068 & 281 & n.a. \\
			EDCA      & 18 & 9.904 & 251 & 31  \\
			Stable    & 21 & 9.826 & 340 & 47  \\
			Guarded   & 21 & 9.778 & 485 & 197 \\
			Emergency & 15 & 9.912 & 273 & 65  \\
			\bottomrule
		\end{tabular}
	\end{center}
	\fonte{Prepared by the author.}
\end{table}

\begin{table}[H]
	\ABNTEXfontereduzida
	\caption{\label{tab:eval:heavy_drop_attr}Heavy-density high-load \gls{mac} drop attribution by access category (100~vehicles, three-run means; counts in thousands, $\times 10^{3}$). Under EDCA and the adaptive profiles, Voice contributes only a small share of total loss; emergency records the fewest Voice-attributed drops among those policies. Under \texttt{plain}, Best Effort and Voice columns are not applicable because all loss is unclassified.}
	\begin{center}
		\small
		\setlength{\tabcolsep}{5pt}
		\begin{tabular}{@{}lrrrr@{}}
			\toprule
			\textbf{Policy} &
			\textbf{BE} &
			\textbf{VO} &
			\textbf{Unclassified} &
			\textbf{Total} \\
			\midrule
			Plain     & n.a. & n.a. & 281 & 281 \\
			EDCA      & 233 & 18 & 0   & 251 \\
			Stable    & 319 & 21 & 0   & 340 \\
			Guarded   & 463 & 21 & 0   & 485 \\
			Emergency & 257 & \textbf{15} & 0   & 273 \\
			\bottomrule
		\end{tabular}
	\end{center}
	\fonte{Prepared by the author.}
\end{table}

\begin{figure}[htb]
	\caption{\label{fig:eval:drop_attr}Packet-drop attribution at \texttt{netload\_high}, heavy density: stacked Best Effort, Voice, and unclassified \gls{mac} drops. Most loss falls on Best Effort; among QoS-enabled policies, emergency yields the smallest Voice share.}
	\begin{center}
		\includegraphics[width=0.95\linewidth]{Figs/fig_05_mac_drop_attribution_high_load_highway_heavy.pdf}
	\end{center}
	\fonte{Prepared by the author.}
\end{figure}

\begin{table}[H]
	\ABNTEXfontereduzida
	\caption{\label{tab:eval:heavy_ctrl}Adaptive-controller actions at heavy-density high load (three-run means; counts in thousands, $\times 10^{3}$). The \texttt{emergency} Best Effort hold entry corresponds to about 170 grant-suppression events.}
	\begin{center}
		\small
		\setlength{\tabcolsep}{5pt}
		\begin{tabular}{@{}lrrr@{}}
			\toprule
			\textbf{Policy} &
			\textbf{VO protection} &
			\textbf{BE drop while blocked} &
			\textbf{BE hold} \\
			\midrule
			Stable    & 93  & 0  & 0 \\
			Guarded   & 94  & 0  & 0 \\
			Emergency & 112 & 22 & 0.17 \\
			\bottomrule
		\end{tabular}
	\end{center}
	\fonte{Prepared by the author.}
\end{table}

\begin{figure}[htb]
	\caption{\label{fig:eval:ctrl}Adaptive-controller actions by offered load, heavy density. Emergency combines Voice-protection activations with explicit Best Effort drops; \texttt{stable} and \texttt{guarded} mainly accumulate protection windows without preemption.}
	\begin{center}
		\includegraphics[width=0.95\linewidth]{Figs/fig_07_v2x_control_actions_by_load_highway_heavy.pdf}
	\end{center}
	\fonte{Prepared by the author.}
\end{figure}

Under \texttt{plain}, the single \gls{dcf} queue reports all loss as unclassified.
Once EDCA classification is enabled, \autoref{tab:eval:heavy_drop_attr} and \autoref{fig:eval:drop_attr} show that almost all drops fall on Best Effort, while Voice-attributed drops remain an order of magnitude smaller (18--21~$\times 10^{3}$ for EDCA/stable/guarded, and 15~$\times 10^{3}$ for emergency).
The same Voice-attributed drop counts appear in \autoref{tab:eval:heavy_vo_loss} alongside reach and Best Effort overflow: emergency is the best QoS-enabled policy for reducing Voice-frame loss, not only for shortening Voice delay.
\texttt{guarded} still has the largest total loss and Best Effort queue-overflow count, showing that suppression without preemption can build Best Effort backlog until queues drop frames.
\texttt{emergency} combines the highest Voice-protection count with explicit Best Effort drops (\autoref{tab:eval:heavy_ctrl}, \autoref{fig:eval:ctrl}) while keeping aggregate \gls{mac} loss close to the \gls{dcf} baseline, so its cost appears as controlled preemption rather than sustained overflow.

\subsection{Load and density as enabling conditions}
\label{sec:eval:heavy:ladder}

\begin{table}[H]
	\ABNTEXfontereduzida
	\caption{\label{tab:eval:heavy_p95}95th-percentile delay (ms) across offered-load regimes for the five-policy ladder (100~vehicles, three-run means with min--max range across seeds; tail percentiles as defined in \autoref{sec:eval:metrics}). Cells with a vanishing seed spread omit the range.}
	\begin{center}
		\scriptsize
		\setlength{\tabcolsep}{3pt}
		\begin{tabularx}{\linewidth}{@{}l *{5}{>{\centering\arraybackslash}X}@{}}
			\toprule
			\textbf{Load} & \textbf{Plain} & \textbf{EDCA} & \textbf{Stable} & \textbf{Guarded} & \textbf{Emerg.} \\
			\midrule
			\multicolumn{6}{@{}l}{\textit{\gls{vo} (crash alert)}} \\
			\texttt{low}    & 0.185 & 0.185 & 0.185 & 0.185 & 0.185 \\
			\texttt{medium} & \shortstack{0.373\\[-1pt]{\scriptsize [0.209--0.562]}} & \shortstack{0.314\\[-1pt]{\scriptsize [0.209--0.435]}} & \shortstack{0.332\\[-1pt]{\scriptsize [0.209--0.419]}} & \shortstack{0.284\\[-1pt]{\scriptsize [0.209--0.340]}} & \shortstack{0.321\\[-1pt]{\scriptsize [0.209--0.386]}} \\
			\texttt{high}   & \shortstack{1.290\\[-1pt]{\scriptsize [0.676--2.515]}} & \shortstack{0.896\\[-1pt]{\scriptsize [0.581--1.479]}} & \shortstack{1.155\\[-1pt]{\scriptsize [0.812--1.646]}} & \shortstack{0.960\\[-1pt]{\scriptsize [0.612--1.525]}} & \shortstack{\textbf{0.461}\\[-1pt]{\scriptsize [0.427--0.507]}} \\
			\addlinespace
			\multicolumn{6}{@{}l}{\textit{\gls{be} (background)}} \\
			\texttt{low}    & 0.217 & 0.217 & \shortstack{27.799\\[-1pt]{\scriptsize [0.217--82.827]}} & \shortstack{28.156\\[-1pt]{\scriptsize [0.217--83.799]}} & 0.217 \\
			\texttt{medium} & \shortstack{0.415\\[-1pt]{\scriptsize [0.297--0.560]}} & \shortstack{0.462\\[-1pt]{\scriptsize [0.323--0.562]}} & \shortstack{1.567\\[-1pt]{\scriptsize [0.504--3.511]}} & \shortstack{0.504\\[-1pt]{\scriptsize [0.358--0.643]}} & \shortstack{35.903\\[-1pt]{\scriptsize [0.386--106.593]}} \\
			\texttt{high}   & \shortstack{57.367\\[-1pt]{\scriptsize [0.678--97.759]}} & \shortstack{155.857\\[-1pt]{\scriptsize [145.173--172.139]}} & \shortstack{144.314\\[-1pt]{\scriptsize [30.734--251.945]}} & \shortstack{174.401\\[-1pt]{\scriptsize [150.061--220.345]}} & \shortstack{98.663\\[-1pt]{\scriptsize [0.693--150.234]}} \\
			\bottomrule
		\end{tabularx}
	\end{center}
	\fonte{Prepared by the author.}
\end{table}

\begin{table}[H]
	\ABNTEXfontereduzida
	\caption{\label{tab:eval:heavy_drop_rate}Normalized \gls{mac} drop rate (drops per application transmission) across strategy and offered load (heavy density, three-run means). High-load cells match the aggregate loss intensity behind \autoref{tab:eval:heavy_drop_attr}.}
	\begin{center}
		\small
		\setlength{\tabcolsep}{5pt}
		\begin{tabular}{@{}lrrr@{}}
			\toprule
			\textbf{Policy} & \textbf{low} & \textbf{medium} & \textbf{high} \\
			\midrule
			Plain     & 0.187 & 0.605 & 1.255 \\
			EDCA      & 0.197 & 0.713 & 1.520 \\
			Stable    & 0.312 & 0.692 & 1.683 \\
			Guarded   & 0.576 & 0.591 & 1.562 \\
			Emergency & 0.260 & 0.804 & \textbf{1.242} \\
			\bottomrule
		\end{tabular}
	\end{center}
	\fonte{Prepared by the author.}
\end{table}

\begin{table}[H]
	\ABNTEXfontereduzida
	\caption{\label{tab:eval:heavy_vo_rx}Voice reception ratio (RX per logical transmission) across strategy and offered load (heavy density, three-run means). High-load fan-out is about 9.07 receivers per alert under \texttt{plain} and 9.78--9.91 under the QoS-enabled policies; the main policy effect under congestion is delay and Voice-loss shaping rather than fan-out collapse.}
	\begin{center}
		\small
		\setlength{\tabcolsep}{5pt}
		\begin{tabular}{@{}lrrr@{}}
			\toprule
			\textbf{Policy} & \textbf{low} & \textbf{medium} & \textbf{high} \\
			\midrule
			Plain     & 9.927 & 9.936 & 9.068 \\
			EDCA      & 9.929 & 9.952 & 9.904 \\
			Stable    & 9.949 & 9.880 & 9.826 \\
			Guarded   & 9.819 & 9.948 & 9.778 \\
			Emergency & 9.945 & 9.952 & 9.912 \\
			\bottomrule
		\end{tabular}
	\end{center}
	\fonte{Prepared by the author.}
\end{table}

\autoref{tab:eval:heavy_p95} explains \emph{when} the policy ladder should be armed.
At \texttt{low} load, Voice P95 is identical across policies, yet \texttt{stable}/\texttt{guarded} inflate Best Effort P95 by two orders of magnitude---bounded blocking without congestion is self-inflicted Best Effort queueing.
The same untimely-protection effect appears for \texttt{emergency} at \texttt{medium} load (Best Effort P95 of 35.9~ms), where its more sensitive trigger (Voice threshold~1) arms preemption against Voice activity that the channel could still absorb; across the \texttt{low} and \texttt{medium} columns the Best Effort tails are therefore non-monotonic in policy aggressiveness, and with three seeds per cell and no interval estimates these off-peak cells should be read as regime indicators rather than precise effect sizes.
By \texttt{high} load, emergency achieves the lowest Voice P95, whereas guarded maximizes Best Effort tail delay.
\autoref{tab:eval:heavy_drop_rate} shows the same enabling condition for loss: normalized \gls{mac} drop rates remain modest at low/medium load and jump at \texttt{high}, where emergency keeps the lowest high-load intensity among the five policies (1.242 drops/TX).
\autoref{tab:eval:heavy_vo_rx} confirms that Voice fan-out remains in that high-single-digit band across the matrix, so the congestion benefit is delay/jitter reduction and fewer Voice-attributed drops (\autoref{tab:eval:heavy_drop_attr}), not a large change in multicast reach.
Cross-reading \autoref{tab:eval:light} and \autoref{tab:eval:heavy_p95} shows that protection windows help only when Voice queueing and channel occupancy are high enough that removing Best Effort competition actually shortens Voice service; below that point, deferring Best Effort merely shifts delay onto a class that did not need protection.
The adaptive profiles are therefore event-triggered escalation levels rather than permanently enabled \gls{qos} modes.

\section{Discussion}
\label{sec:eval:discussion}

\subsection{Event-triggered escalation, not always-on QoS}
\label{sec:eval:disc:escalation}

The light-density sweep and the heavy-density load ladder jointly show that adaptive blocking is harmful when armed under low channel stress and useful only when congestion makes Best Effort competition material to Voice service.
In other words, the mechanism should be treated as an escalation level attached to genuine crash-alert activity, not as an always-on replacement for ordinary EDCA marking~\cite{IEEE80211e_2005,aguiar2026_veins_qos}.
Explicit DSCP-to-Voice mapping remains a reasonable default; bounded or emergency suppression should be reserved for periods when the channel is already contested.

\subsection{Why Best Effort and Voice delays differ under plain DCF}
\label{sec:eval:disc:plain}

Under \texttt{plain}, both classes receive identical per-frame \gls{mac} treatment (one \gls{dcf} queue, no classifier), yet \autoref{tab:eval:heavy_qos} shows different per-class statistics because the classes are sampled differently.
\autoref{tab:eval:plain_process} records the two processes: Best Effort pools one hundred senders over the whole~\SI{70}{\second} run with 420~B payloads, whereas Voice originates from a single stationary crash node during the~\SI{30}{\second} alert window with 260~B payloads.
\autoref{fig:eval:plain_class_p95} makes the consequence visible seed by seed at \texttt{plain\_netload\_high}: seed~\texttt{\#0} has almost identical Voice and Best Effort P95 (0.676 versus 0.678~ms), while seeds~\texttt{\#1} and~\texttt{\#2} leave Voice near 1--2.5~ms and stretch Best Effort to 74--98~ms.
The mean Best Effort P95 of 57~ms is therefore a mixture of one uncongested seed and two saturated seeds, not evidence that the single \gls{dcf} queue differentiates access categories.
Cross-class comparisons within one policy therefore describe two different traffic processes; cross-policy comparisons within one class remain the controlled measurement.

\begin{table}[htb]
	\ABNTEXfontereduzida
	\caption{\label{tab:eval:plain_process}Traffic processes under heavy-density \texttt{plain\_netload\_high}. Both classes share one \gls{dcf} queue; only the generation process differs.}
	\begin{center}
		\small
		\begin{tabular}{@{}lrrr@{}}
			\toprule
			\textbf{Class} & \textbf{Senders} & \textbf{Active window} & \textbf{Payload} \\
			\midrule
			Best Effort & 100 & \SI{70}{\second} (whole run) & 420~B \\
			Voice & 1 (crash node) & \SI{30}{\second} alert window & 260~B \\
			\bottomrule
		\end{tabular}
	\end{center}
	\fonte{Prepared by the author.}
\end{table}

\begin{figure}[htb]
	\caption{\label{fig:eval:plain_class_p95}Voice versus Best Effort P95 delay under \texttt{plain\_netload\_high} (heavy density), shown per seed. Seed~\texttt{\#0} is uncongested for both classes; seeds~\texttt{\#1}--\texttt{\#2} explode the Best Effort tail while Voice remains near a few milliseconds.}
	\begin{center}
		\includegraphics[width=0.92\linewidth]{Figs/plain_class_p95.pdf}
	\end{center}
	\fonte{Prepared by the author.}
\end{figure}

\subsection{Why Voice reach exceeds Best Effort reach}
\label{sec:eval:disc:reach}

Voice RX/alert (about 9.07 under \texttt{plain} and 9.78--9.95 under the QoS-enabled policies) is higher than Best Effort RX/TX (about 6.15 under \texttt{plain}) for reasons that are not policy effects.
The denominators differ: Voice divides received copies by logical alert sequences, whereas Best Effort divides received copies by physical packet transmissions.
The crash node is stopped in a denser neighborhood than the moving fleet average, and Best Effort averages include the early sparse-injection phase before all one hundred vehicles have entered the corridor.
Those geometry and counting differences---not a hidden class bias in \texttt{plain} \gls{dcf}---explain why Voice fan-out sits in the high single digits while Best Effort sits near six.

\subsection{Practical significance of the observed Voice gains}
\label{sec:eval:disc:significance}

The absolute Voice delays are small: even the plain baseline keeps Voice P95 near 1.3~ms at \texttt{netload\_high}, well below representative V2X latency budgets such as the~\SI{25}{\milli\second} end-to-end bound for cooperative platooning in \gls{threegpp} TS~22.186~\cite{ts22186}.
The reported reductions therefore quantify each policy's tail-shaping headroom---and its cost---rather than an application-level safety benefit in the simulated scenario.
Under harsher conditions the same contention that produces 57--174~ms Best Effort tails would act on alert traffic as well; the study accordingly claims \gls{mac}-level tail shaping under synthetic saturation, not improved crash outcomes.

\subsection{Protection cost and the coarse Best Effort class}
\label{sec:eval:disc:cost}

The clearest cost signal is that every protection mechanism makes Best Effort service worse than plain \gls{dcf} at high load (Best Effort P95 from 57~ms to 99--174~ms), and \texttt{emergency} converts part of the backlog into explicit loss.
Because the model treats \emph{all} Best Effort traffic as suppressible, these penalties are a worst-case bound for the two-class abstraction (\autoref{sec:sys:scope}): in a real stack, traffic outside the current crash alert may still be safety relevant, so a binary Voice/Best Effort split can underestimate preemption harm.
Deployments would need finer-grained classes before comparable \gls{mac} aggression, and the trade-off is defensible only for genuinely critical, short-lived flows such as the alert window studied here.

\subsection{Fairness of the Best Effort penalty}
\label{sec:eval:disc:spatial}

The reported Best Effort statistics pool all vehicles, but the penalty is unlikely to be uniform: protection is armed locally by overheard Voice frames, so vehicles near the crash source defer or drop Best Effort traffic while distant vehicles continue unaffected (\autoref{sec:sys:controller}).
The Best Effort cost is thus spatially concentrated around the event---arguably desirable for a crash---but pooled averages understate the worst-affected vehicles; per-vehicle fairness is left as future work.

\subsection{Denial-of-service risk of Voice-triggered suppression}
\label{sec:eval:disc:dos}

Protection is armed by local Voice queue pressure and \emph{overheard} Voice frames without authenticating the sender, so a faulty or malicious node continuously emitting Voice-marked traffic could keep nearby vehicles in blocking mode, deferring or---under \texttt{emergency}---dropping their Best Effort traffic.
The risk is bounded but not removed: protection windows are finite (20--80~ms maximum continuous block plus a guard interval, \autoref{tab:impl:parameters}) and suppression affects only a node's own Best Effort transmissions, not reception.
Real deployment would additionally require V2X message authentication, rate limiting of alert bursts, and plausibility checks before Voice frames may trigger suppression.

\subsection{Limitations and threats to validity}
\label{sec:eval:disc:limits}

Heavy-density \glspl{kpi} average three seeds and report the min--max range on headline delay tables, still without confidence intervals or significance tests, so results are qualitative mechanism checks rather than proven effect sizes; all observed prioritization is statistical contention shaping, not deterministic real-time communication~\cite{kosekszott2012What}.
The evaluation is simulation-only, models a single crash source on a one-hop highway corridor, and uses the simplified \gls{phy} assumptions of \autoref{chap:implementation} and \autoref{sec:sys:scope}; concurrent alerts would arm overlapping protection windows whose interaction is not evaluated.
Results do not predict absolute performance under richer propagation or congestion control, and no adaptive-EDCA or \gls{dcc} baseline is implemented, so no superiority claim is made against those families~\cite{etsi_dcc_2018}.
The configured \gls{edca} parameters follow the IEEE~802.11 BSS QoS defaults rather than the distinct \gls{ocb} default set, and group-addressed frames use \gls{inet}'s default fastest-mandatory-mode rate selection rather than a basic-rate policy (\autoref{sec:impl:radio}); both choices are uniform across policies but may shift absolute values relative to deployed 802.11p stacks.
The \texttt{high} crash-burst profile is a deliberate stress case rather than a standardized alert schedule, and no application-level safety deadline is evaluated.
The three adaptive profiles are fixed design points whose triggers are not individually ablated, although their spread of block windows (4--15~ms) and Voice thresholds (1--3 packets) acts as a coarse sensitivity ladder; a systematic parameter sweep is future work.
Within that scope the comparison remains internally fair: all configurations share the same \gls{phy} stack, mobility trace, and load matrix, isolating \gls{mac}-policy effects~\cite{aguiar2026_veins_qos}.

%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%
\section{Chapter Summary}
\label{sec:eval:summary}

The evaluation compared five \gls{mac} policies on the same highway matrix and found a conditional protection-versus-cost frontier.
Under heavy density and high load, emergency preemption reduces Voice P95 by about 49\% relative to EDCA (0.90~ms to 0.46~ms), collapses Voice jitter to 0.047~ms, and records the fewest Voice-attributed \gls{mac} drops among the QoS-enabled policies (15~$\times 10^{3}$ versus 18--21~$\times 10^{3}$), while Best Effort P95 rises from 57~ms under plain \gls{dcf} to 99~ms; under light contention, bounded suppression mainly harms Best Effort without improving alerts.
The practical takeaway is therefore conditional: explicit DSCP-to-Voice marking is a reasonable default, whereas adaptive blocking---especially emergency preemption---is justified only as an event-triggered escalation under genuine congestion, with authentication and rate limiting as deployment prerequisites.
The absolute Voice delays in this synthetic setting remain far below representative platooning budgets~\cite{ts22186}; the contribution is \gls{mac}-level tail shaping under controlled saturation, not a claim of application-level crash-outcome improvement.

The subsequent chapter concludes the dissertation by summarizing these findings, stating the conditional practical takeaway, and outlining limitations and future work.
